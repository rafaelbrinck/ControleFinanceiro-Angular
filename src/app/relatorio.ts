export class Relatorio {
  entradas: number;
  saidas: number;
  resultado: number;

  constructor(entrada: number, saida: number, resultado: number) {
    this.entradas = entrada;
    this.saidas = saida;
    this.resultado = resultado;
  }
}
